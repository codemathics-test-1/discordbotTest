# merge from change stack

scratch pr to validate merging a real pr from the change stack ui (hutch preview). docs-only, safe to merge — merging it IS the test.

this is a test on the merge capabilities.

this is a test on the merge capabilities.
